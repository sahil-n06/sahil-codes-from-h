#include<stdio.h>
#include<math.h>
int sum(int n)
{
  int sum=0,i,j,k,t,fact;
  for(i=1;i<n+1;i++)
  {
    t=pow(i+1,i);
    for(j=1;j<i+1;j++)
    {
      fact=fact*
    }
  }
}
void main()
{
  int n,t;
  printf("Enter the nth position you want to add the series : ");
  scanf("%d",&n);
  t=sum(n);
}
